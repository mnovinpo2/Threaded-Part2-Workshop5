@{
    ViewData["Title"] = "Home";
    
}

<div class="">
    <div class="jumbo text-center my-5">
        <div class="overlay h-100">
            <div class="d-flex justify-content-center align-items-center h-100">
                <div class="">
                    <h1 class="mb-3 text-white">Welcome To Travel Experts</h1>
                    <h3 class="mb-3 text-white">See What we Have To offer</h3>
                    <a class="btn btn-outline-light btn-lg" role="button">Check it out</a>
                </div>
            </div>
        </div>
    </div>

    @if (User.Identity.IsAuthenticated)
    {
        <form action="@Url.Action("Bookings", "TravelProduct")">
            <button id="booking" type="submit" class="col-6 offset-3">Bookings</button>
        </form>
    }

</div>
